# FastAPI & server
fastapi
uvicorn[standard]
python-multipart

# MongoDB
motor
pymongo
certifi

# Supabase
supabase

# ML/AI for e-learning model training
scikit-learn
pandas
numpy
joblib

# Caching
redis

# Media/file storage
cloudinary

# Curriculum crawling (ml.data.curriculum.crawler) — parses the TDTU syllabus
# pages. httpx is already a transitive dep of FastAPI / Supabase, pinned here
# to make the crawler's direct dependency explicit.
beautifulsoup4
lxml
httpx